language: rust
rust:
  - stable
  - nightly

cache: cargo

jobs:
  include:
    - stage: "Test"
      script: cargo test --verbose --all

    - stage: "Cli Test"
      script: /bin/sh ./tests.sh

    - stage: "Build"
      script: cargo build --release --verbose --all

